{
  "id": "gen-image-gpt",
  "capability": "image",
  "kind": "model",
  "rank": 10,
  "detect": {
    "files": [
      "~/scripts/gen-image.py"
    ],
    "bin": "codex"
  },
  "io": {
    "in": [
      "text"
    ],
    "out": "image"
  },
  "run": "python3 ~/scripts/gen-image.py generate --provider chatgpt --prompt \"$(cat {in})\" --out {out}",
  "timeout_s": 600,
  "window_h": 5,
  "quota_patterns": [
    "usage limit",
    "rate limit",
    "quota"
  ],
  "notes": {
    "en": "gpt-image-2 through Codex OAuth, no API key. Primary for any picture. window_h=5 is the ChatGPT subscription window; soft_cap is unset because the image limit is unknown, exhaustion is detected from stderr",
    "ru": "gpt-image-2 через Codex OAuth, без API-ключа. primary для любых картинок. window_h=5 — окно подписки ChatGPT; soft_cap не задан: лимит картинок неизвестен, исчерпание ловится по stderr"
  }
}
